# this will load up the git submodules
# only needed if you're hacking on flex
# or you could have cloned with 
# git clone --recursive
# https://git-scm.com/book/en/v2/Git-Tools-Submodules#Cloning-a-Project-with-Submodules

git submodule init
git submodule update
git submodule status